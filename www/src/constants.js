export const REQUEST_TIMEOUT = 2000;
export const CONNECTION_TIMEOUT_DELAY_STEP = 500;
export const CONNECTION_TIMEOUT_MAX_DELAY = 5000;
export const CONNECTION_CLOSE_TIMEOUT = 500;

export const REQUEST_SIGNATURE = [0x34, 0xaa];
export const DEFAULT_ADDRESS = "esp_lamp.local";

export const THROTTLE_INTERVAL = 1000 / 60;